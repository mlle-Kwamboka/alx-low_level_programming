#include "main.h"

/**
 * _pow_recursion - returns the value of x to power y
 * @x: the base
 * @y: the power
 *
 * Return: x to power y value or -1 if y is lower than 0
 */
int _pow_recursion(int x, int y)
{
	int i, j;
	
	if ( i == x && j == y)
	{
		if (j != 0)
			return (x * x)
	}
}
