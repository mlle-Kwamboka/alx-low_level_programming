#include <stdio.h>
/**
 * main - prints out exactly with proper grammar,
 * but the outcome is a piece of art
 * if exited properly
 * Return: Always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

